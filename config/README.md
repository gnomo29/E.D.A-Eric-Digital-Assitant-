# Configuración

Esta carpeta se reserva para configuración versionada del proyecto (no sensible).

- Use `.env` en la raíz para secretos locales (no subir a Git).
- Puede colocar aquí futuros perfiles (`dev.toml`, `prod.toml`) sin mezclar con `src/`.
